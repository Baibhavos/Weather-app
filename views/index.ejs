<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300" />
    <title>Document</title>
  </head>

  <body>
    <style>
      .container {
        height: 400px;
        width: 600px;
        border-style: solid 2px red;
        background-color: rgb(211, 235, 235);
        border-radius: 5px;
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
      }

      .inpt {
        height: 50px;
        width: 350px;
        margin-left: 30px;
        margin-top: 20px;
        border-radius: 5px;
        border-style: hidden;
        text-align: center;
        font-size: 30px;
      }

      .btn {
        height: 50px;
        width: 150px;
        margin-left: 20px;
        font-size: 20px;
        border-radius: 50px;
        border-style: hidden;
        z-index: 2;
        transition: 0.2s;
      }

      .btn:hover {
        box-shadow: 0px 12px 16px 0px rgb(204, 198, 198);
      }
    </style>
    <div class="container">
      <form action="/" method="post">
        <input
          name="city"
          type="text"
          class="inpt"
          placeholder="Enter a City"
          required
        />
        <input type="submit" class="btn" value="Get Weather" />
      </form>
      <% if(weather !== null){ %>
      <p
        style="
          font-size: 55px;
          margin: top 30px;
          margin-left: 30px;
          margin-right: 25px;
        "
      >
        <%= weather %>
      </p>
      <% } %> <% if(error !== null){ %>
      <p><%= error %></p>
      <% } %>
    </div>
  </body>
</html>
